<config version="v1">
    <chain template=<%= %q["] %><%= p('binarystore.provider') %><%= %q["] %>/>
    <provider id=<%= %q["] %><%= p('binarystore.provider') %><%= %q["] %> type=<%= %q["] %><%= p('binarystore.provider') %><%= %q["] %>>
        <bucketName><%= p('binarystore.bucketname') %></bucketName>
        <providerId><%= p('binarystore.provider') %></providerId>
        <endpoint><%= p('binarystore.endpoint') %></endpoint>
        <region><%= p('binarystore.region') %></region>
        <identity><%= p('binarystore.identity') %></identity>
        <credential><%= p('binarystore.credential') %></credential>
        <httpsOnly>true</httpsOnly>
    </provider>
</config>